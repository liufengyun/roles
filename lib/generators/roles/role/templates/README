===============================================================================

Now, you just have to run the migration using rake command:

   rake db:migrate

An initializer file has been created here: config/initializers/roles.rb, 
you can change roles settings to match your needs. 
Defaults values are commented out.

===============================================================================
